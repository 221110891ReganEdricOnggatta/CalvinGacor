import 'package:flutter/material.dart';

class MySends extends StatelessWidget {
  const MySends({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("data"),
      ),
    );
  }
}
